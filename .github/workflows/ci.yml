name: CI

on:
  pull_request:

permissions:
  contents: write

env:
  CARGO_TERM_COLOR: always

concurrency:
  group: ci-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  align-windows-lock:
    if: github.head_ref == 'integrate-studio-portable'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@main
        with:
          ref: integrate-studio-portable
          fetch-depth: 0
      - name: Force gpu-allocator and WGPU onto Windows 0.62
        run: cargo update -p windows@0.61.1 --precise 0.62.0
      - name: Commit Cargo.lock
        run: |
          git config user.name "rEspanso Dependency Bot"
          git config user.email "actions@users.noreply.github.com"
          git add Cargo.lock
          if git diff --cached --quiet; then
            echo "Cargo.lock is already unified"
            exit 0
          fi
          git commit -m "fix: unify Windows bindings for WGPU"
          git push origin HEAD:integrate-studio-portable
