name: Diagnose Windows icon build

on:
  pull_request:
    branches:
      - rEspanso-feature
    paths:
      - .github/workflows/diagnose-windows-icon-build.yml

permissions:
  contents: read

jobs:
  capture:
    runs-on: windows-2022
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4
      - uses: ilammy/msvc-dev-cmd@v1
        with:
          arch: x64
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-pc-windows-msvc
      - uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: diagnose-windows-icon-${{ hashFiles('**/Cargo.lock') }}
      - name: Capture exact release build
        id: build
        continue-on-error: true
        shell: pwsh
        run: |
          $ErrorActionPreference = "Continue"
          cargo build --release -p espanso --bin espanso --bin respanso-portable *>&1 | Tee-Object -FilePath windows-icon-build.log
          if ($LASTEXITCODE -ne 0) { exit 1 }
      - uses: actions/upload-artifact@v4
        with:
          name: windows-icon-build-log-${{ github.sha }}
          path: windows-icon-build.log
          if-no-files-found: error
          retention-days: 3
      - name: Preserve result
        if: steps.build.outcome == 'failure'
        run: exit 1
